let onUnauthorized=()=>{}; export const setUnauthorizedHandler=(fn)=>{onUnauthorized=fn};
export async function api(url,opts={}){const token=localStorage.getItem('clearpath_token');const headers={'Content-Type':'application/json',...(opts.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;const r=await fetch(url,{...opts,headers});if(r.status===401&&url!=='/api/auth/login'){onUnauthorized();}if(!r.ok){const e=await r.json().catch(()=>({}));const err=new Error(e.error||'Request failed');err.status=r.status;throw err;}return r.status===204?null:r.json();}
